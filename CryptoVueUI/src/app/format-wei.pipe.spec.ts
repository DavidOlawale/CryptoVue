import { FormatWeiPipe } from './format-wei.pipe';

describe('FormatWeiPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatWeiPipe();
    expect(pipe).toBeTruthy();
  });
});
